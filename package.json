{
  "name": "filemananager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "yarn && yarn --cwd storage && cp storage/.env.sample storage/.env",
    "dc": "docker-compose",
    "start": "yarn dc up -d && yarn --cwd storage start",
    "stop:docker": "yarn dc stop && yarn --cwd storage stop",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC"
}
